# Arts by Justin

> **CLAUDE.md belongs in version control — NEVER add it to .gitignore. This file is the shared source of truth for all developers and all Claude Code sessions.**

This site: Arts by Justin | Repo: github.com/Spirit-Media-US/artsbyjustin | Domain: artsbyjustin.com | Sanity ID: oqoqh3p3 | R2 bucket: n/a (planned for artwork images)

## Dev Commands

- `npm run dev` — local preview at localhost:4321
- `npm run build` — production build to dist/

## Status

- Deployed to Netlify at artsbyjustin.netlify.app
- Remaining: connect custom domain, add Cloudflare proxy, replace image placeholders with R2 artwork, replace placeholder divs with GHL form embeds

## Rules

- All work goes to the **dev** branch — never push directly to main
- Only merge dev to main when Kevin says "push to main"
- Never push without local preview first
