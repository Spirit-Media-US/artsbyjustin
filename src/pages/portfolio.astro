---
import Layout from '../layouts/Layout.astro';
import { sanityClient, urlFor } from '../lib/sanity';

let sanityArtworks: object[] = [];
try {
  const raw = await sanityClient.fetch(`
    *[_type == "artwork" && defined(image)] | order(order asc, _createdAt desc) {
      _id, title, image, category, medium, dimensions, year, price, sold, featured, description
    }
  `);
  sanityArtworks = raw.map((a: any) => ({
    id: a._id,
    imgUrl: urlFor(a.image).width(1400).url(),
    thumbUrl: urlFor(a.image).width(400).url(),
    title: a.title || 'Untitled',
    category: a.category || '',
    medium: a.medium || '',
    size: a.dimensions || '',
    year: a.year || '',
    sold: a.sold || false,
    price: a.sold ? 'SOLD' : (a.price ? `$${Number(a.price).toLocaleString()}` : 'Inquire'),
    desc: a.description || '',
    featured: a.featured || false,
  }));
} catch(e) {
  console.error('Portfolio Sanity fetch error:', e);
}
---
<Layout title="Portfolio" description="The full collection — bold acrylic paintings, mixed media works, resin art, murals, and 3D pieces by Justin Keishing. Each one a story made with purpose.">

<!-- HERO -->
<section class="hero">
  <div class="hero-tag"><div class="hero-line"></div><span>The Collection</span><div class="hero-line"></div></div>
  <h1>Art That<br/><em>Moves You.</em></h1>
  <p>Bold acrylic paintings, mixed media works, resin art, murals, and 3D pieces — each one a story, each one made with purpose.</p>
</section>

<!-- FILTER -->
<div class="filter-bar">
  <button class="filter-btn active" data-cat="all">All Works</button>
  <button class="filter-btn" data-cat="Sports & Motion">Sports & Motion</button>
  <button class="filter-btn" data-cat="Portraits & Identity">Portraits & Identity</button>
  <button class="filter-btn" data-cat="Faith & Spirit">Faith & Spirit</button>
  <button class="filter-btn" data-cat="Abstract & Mixed Media">Abstract & Mixed Media</button>
</div>

<!-- CINEMATIC VIEWER -->
<div class="cinematic" id="cinematicViewer">
  <button class="cinema-fullview" id="fullViewBtn">⤢ Full View</button>
  <div class="cinema-nav">
    <div class="cinema-arrow" id="prevBtn">↑</div>
    <div class="cinema-arrow" id="nextBtn">↓</div>
  </div>
  <div class="cinema-counter">
    <div class="counter-current" id="counterCurrent">01</div>
    <div class="counter-total" id="counterTotal">of 20</div>
  </div>
  <div class="cinema-progress"><div class="cinema-progress-fill" id="progressFill"></div></div>
</div>
<div class="thumb-strip" id="thumbStrip"></div>

<!-- STATS BAR -->
<div class="stats-bar">
  <div><div class="stat-num">460+</div><div class="stat-label">Original Works</div></div>
  <div><div class="stat-num">5</div><div class="stat-label">Series & Collections</div></div>
  <div><div class="stat-num">3</div><div class="stat-label">Countries Exhibited</div></div>
  <div><div class="stat-num">∞</div><div class="stat-label">Stories Told</div></div>
</div>

<!-- PORTFOLIO GRID -->
<section class="grid-section">
  <div class="grid-header reveal">
    <div><p class="label">Complete Collection</p><div class="bar"></div><h2>All Works</h2></div>
    <div style="display:flex;align-items:center;gap:1.5rem;">
      <div style="display:flex;align-items:center;gap:0.5rem;font-size:0.72rem;color:var(--gray2);letter-spacing:1px;"><div style="width:10px;height:10px;background:rgba(180,30,30,0.9);"></div>Sold</div>
      <div style="display:flex;align-items:center;gap:0.5rem;font-size:0.72rem;color:var(--gray2);letter-spacing:1px;"><div style="width:10px;height:10px;border:1px solid var(--gold);"></div>Available</div>
    </div>
  </div>
  <div class="portfolio-grid" id="portfolioGrid"></div>
</section>

<!-- CHASE YOUR DREAMS SERIES -->
<section class="series" id="chaseSeries" style="display:none">
  <div class="series-intro reveal">
    <div>
      <p class="label">Featured Series</p><div class="bar"></div>
      <h2>Chase Your Dreams</h2>
      <p>Justin's most celebrated series — bold acrylic paintings of athletes, musicians, and dreamers, each in grayscale with an explosive burst of color representing their purpose.</p>
      <p style="margin-top:1rem;color:var(--gray2);font-size:0.93rem;line-height:1.9;font-weight:300;">"This series was born out of my own ambition to chase my dreams no matter what happens."</p>
      <button class="btn-gold" style="margin-top:1.5rem;" id="shopChaseBtn">Shop This Series →</button>
    </div>
    <div id="seriesHeroImg" style="aspect-ratio:3/4;background-size:cover;background-position:center top;position:relative;">
      <div style="position:absolute;bottom:1rem;right:1rem;font-size:0.65rem;letter-spacing:2px;color:rgba(255,255,255,0.5);text-transform:uppercase;">SOAR · Acrylic on Canvas</div>
    </div>
  </div>
  <div class="series-grid" id="chaseGrid"></div>
</section>

<!-- COLLECTORS WALL -->
<section class="collectors" id="collectorsWall">
  <div class="collectors-header reveal">
    <div>
      <p class="label">Happy Collectors</p><div class="bar"></div>
      <h2>A Wall of<br/><em>Happy Collectors.</em></h2>
    </div>
    <p class="collectors-sub">From exhibitions to living rooms — these are the people who've welcomed Justin's work into their lives. Every face tells a story. Every painting found its person.</p>
  </div>
  <div class="ticker-wrap" id="tickerWrap"></div>
  <div style="text-align:center;margin-top:2rem;">
    <p style="font-size:0.75rem;letter-spacing:2px;text-transform:uppercase;color:var(--gray2);margin-bottom:1.2rem;">Own a Justin Keishing original? We'd love to feature you.</p>
    <button class="btn-ghost" id="submitPhotoBtn">Submit Your Photo →</button>
  </div>
</section>

<!-- BEYOND THE CANVAS -->
<section class="beyond" id="beyondCanvas">
  <div class="beyond-header reveal">
    <p class="label">Large-Scale & Commercial</p><div class="bar"></div>
    <h2>Beyond the Canvas —<br/><em>Built to Last.</em></h2>
    <p>From full salon interiors to sculptural concrete installations, Justin brings the same fearless creativity to large-scale and commercial projects. These aren't decorations — they're experiences.</p>
  </div>
  <div class="projects-grid" id="projectsGrid"></div>
  <div class="beyond-cta reveal">
    <h3>Have a Space That Needs a Statement?</h3>
    <p>Justin works with businesses, interior designers, and property developers on bespoke large-scale installations.</p>
    <button class="btn-gold" id="muralInquiryBtn">Discuss Your Project →</button>
  </div>
</section>

<!-- COMMISSION BAND -->
<section class="comm-band reveal">
  <div>
    <p class="label">Custom Work</p><div class="bar"></div>
    <h2>Don't See What<br/>You're Looking For?<br/><em>Let's Create It.</em></h2>
    <p>Justin accepts a limited number of commissions each year. Every commissioned piece is personal, intentional, and built around your story.</p>
    <a href="/commissions" class="btn-gold" style="display:inline-block;text-decoration:none;">Start a Commission →</a>
  </div>
  <div>
    <p class="label" style="margin-bottom:1.5rem;">How It Works</p>
    <div class="comm-steps-mini">
      <div class="comm-step-mini"><div class="step-dot">01</div><p>Share your idea, vision, or the story you want told</p></div>
      <div class="comm-step-mini"><div class="step-dot">02</div><p>Justin consults on concept, size, medium &amp; timeline</p></div>
      <div class="comm-step-mini"><div class="step-dot">03</div><p>Your one-of-a-kind piece is created with care &amp; purpose</p></div>
      <div class="comm-step-mini"><div class="step-dot">04</div><p>Delivered ready to hang — and to move people</p></div>
    </div>
  </div>
</section>

<!-- LIGHTBOX -->
<div class="lightbox" id="lightbox">
  <div class="lb-inner" id="lbInner">
    <div class="lb-art" id="lbArt"><button class="lb-close" id="lbCloseBtn">✕</button></div>
    <div class="lb-info">
      <div>
        <div class="lb-nav">
          <button class="lb-nav-btn" id="lbPrevBtn">← Prev</button>
          <button class="lb-nav-btn" id="lbNextBtn">Next →</button>
        </div>
        <div class="lb-cat" id="lbCat"></div>
        <div class="lb-title" id="lbTitle"></div>
        <div class="lb-meta" id="lbMeta"></div>
        <div class="lb-divider"></div>
        <div class="lb-desc" id="lbDesc"></div>
        <div class="lb-price-row"><div class="lb-price" id="lbPrice"></div><div class="lb-price-note" id="lbPriceNote"></div></div>
      </div>
      <div class="lb-actions" id="lbActions"></div>
    </div>
  </div>
</div>

<!-- INQUIRY MODAL -->
<div class="inquiry-modal" id="inquiryModal">
  <div class="inq-box" id="inqBox">
    <button class="inq-close" id="inqCloseBtn">✕</button>
    <div id="inqForm">
      <div class="inq-title">Get in Touch</div>
      <div class="inq-sub">Justin will respond within 2–3 business days</div>
      <div class="inq-for" id="inqFor" style="display:none;"></div>
      <div class="form-grid">
        <div class="form-row"><label>First Name</label><input type="text" id="inqFirst" placeholder="Jordan"/></div>
        <div class="form-row"><label>Last Name</label><input type="text" id="inqLast" placeholder="Smith"/></div>
      </div>
      <div class="form-row"><label>Email</label><input type="email" id="inqEmail" placeholder="you@example.com"/></div>
      <div class="form-row">
        <label>I'm interested in…</label>
        <select id="inqType">
          <option value="purchase">Purchasing an existing work</option>
          <option value="commission">Commissioning a custom piece</option>
          <option value="mural">Mural / large-scale project</option>
          <option value="general">General inquiry</option>
        </select>
      </div>
      <div class="form-row"><label>Message</label><textarea id="inqMsg" placeholder="Tell Justin about what you're looking for…"></textarea></div>
      <button class="btn-gold" style="width:100%;padding:1rem;" id="inqSubmitBtn">Send Inquiry →</button>
    </div>
    <div class="form-success" id="inqSuccess" style="display:none;">
      <div class="check">✦</div>
      <h3>Message Sent</h3>
      <p>Thank you for reaching out. Justin will be in touch within 2–3 business days.</p>
    </div>
  </div>
</div>

</Layout>

<script is:inline define:vars={{ sanityArtworks }}>
// ── ARTWORK DATA (from Sanity) ─────────────────────────────────
const artworks = sanityArtworks;


const customerPhotos = [
  'https://cdn.sanity.io/images/oqoqh3p3/production/3836fdc614b3b3e280b83cf3c339074093fb655e-4284x5712.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/d720982673bc3290132da02f2b8071549b3ccca3-3024x4032.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/5d2ec46e2e5cfa3685db7f3958c50d1d92c6931c-3024x4032.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/8e621a569a2a22f93a64fc358870389d82453f1b-3024x4032.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/5636b344f5b99096f9f3551a2da02ff5246d48cb-4284x5712.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/646f674331a9701e2c2620f1d895abe794a69330-4284x5712.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/666b481df99efd28b9a6698de7b16508c078a53b-2996x4004.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/75b236474382bc643fd2c62b4f3a121c2a68e949-3000x3750.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/eeb8bed1d869696081ee78db186ba0c53a15ae07-1320x2868.png',
  'https://cdn.sanity.io/images/oqoqh3p3/production/07cb3b974d4831950417dd2c94ce149c9b262aba-4284x5712.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/cb67cc0729c6a15e90aa2dc4969f7ea82f889eea-3010x3988.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/f062a8db32b5ccb1254a636b38f2eb173dcc7fc8-2448x3264.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/82746e92be9e125ae8ced3e4cfcfc255cb4db52c-2550x3174.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/6e695a0a725c90d87c71b62e547f2d65473a3fb2-3000x3831.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/705da5eea18ec52554feb1816360bc83611a13ed-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/27b0d479b5604073faaba6dcb06adaf3c4c418d0-3024x4032.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/00e90057c5f4915b654cab13c3870487834ecfcc-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/70fbf199bc0e19fce5a834836f8068c4071d196c-1284x1074.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/642effadd89ddb2926c9b6454cc27bbb08ecdbfe-2579x3230.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/a7c0b74c41bca3fc3d0a0a9350328177337a1496-2996x4004.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/0f94ef4c2f235b3e199025f437b4c2eaf1722349-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/80279dc4971eb2d458ea934ed16e58ca01a642de-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/c6c3d860d51ee8219979752adcbfa56dcd75102d-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/e987cd919f97e68f4fe3efc4a3d0887535ec3ee1-3464x3464.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/d08d879667c305a4dc3b8a93ec47e203a122fd75-3277x4096.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/795cc6b065f7aa4d1bc6ef9aed42acde948196ff-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/d9844525a3c7649ce9f87c30b3fdcf7e088da166-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/c86232a34b98632964dc22116ed22e45746f280a-1834x2448.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/a94f40dfa0adfe78ecc05dc6f7ae7c1655b55e34-3000x4000.jpg',
  'https://cdn.sanity.io/images/oqoqh3p3/production/a79ce3da761cad866e48b67bbde8d10bb08f9f93-2574x3710.jpg',
];

const projects = [
  {url:'https://cdn.sanity.io/images/oqoqh3p3/production/5f54facbc75e5946d1d3ea20ab4e43c3dabc1f21-3755x2816.jpg', type:'Salon Interior', title:'Studio Transformation', desc:'Full wall installation, acrylic & mixed media', layout:'wide'},
  {url:'https://cdn.sanity.io/images/oqoqh3p3/production/a44698f7c907bd6799da160ca1dcb37ba36ad941-4096x4096.jpg', type:'3D Mixed Media', title:'Sculptural Wall Piece', desc:'Dimensional mixed media, custom commission', layout:'sq'},
  {url:'https://cdn.sanity.io/images/oqoqh3p3/production/3a2813d5a3d0e2f1b313d5fa9248c6500eecf4ff-3024x4032.jpg', type:'Concrete & 3D', title:'Architectural Gate', desc:'Sculpted concrete relief, outdoor installation', layout:'sq'},
  {url:'https://cdn.sanity.io/images/oqoqh3p3/production/74b15683413c33987fc1ccc2328d82bf3ffb6a2f-1284x945.jpg', type:'Salon Interior', title:'Client Space · Before & After', desc:'Complete interior art transformation', layout:'wide'},
  {url:'https://cdn.sanity.io/images/oqoqh3p3/production/9dcd8a7ae46001f24244916b732e51ea8175162e-4096x4096.jpg', type:'3D Mixed Media', title:'Custom Relief Installation', desc:'Mixed media, resin & sculptural elements', layout:'sq'},
  {url:'https://cdn.sanity.io/images/oqoqh3p3/production/baeaed0b2287fa4fca8990e874a61ebd9997f8fe-4066x5421.jpg', type:'Concrete / Door', title:'Feature Door', desc:'Sculpted concrete finish, private residence', layout:'sq'},
];

// ── HELPERS ───────────────────────────────────────────────────
function getBg(a){return a.imgUrl?`url('${a.imgUrl}') center top/cover no-repeat`:'#1a1a1a';}
function getThumb(a){return a.thumbUrl?`url('${a.thumbUrl}') center top/cover no-repeat`:'#1a1a1a';}
function getCategoryName(c){return{'Sports & Motion':'Sports & Motion','Portraits & Identity':'Portraits & Identity','Faith & Spirit':'Faith & Spirit','Abstract & Mixed Media':'Abstract & Mixed Media'}[c]||'Collection';}
function getCategoryShort(c){return{'Sports & Motion':'Sports','Portraits & Identity':'Portraits','Faith & Spirit':'Faith','Abstract & Mixed Media':'Mixed Media'}[c]||c||'';}

// ── STATE ─────────────────────────────────────────────────────
let currentSlide=0, filteredArtworks=[...artworks], currentLbIndex=0, lbWorks=[...artworks];
let autoPlay, progressTimer, progressWidth=0;
const SLIDE_DURATION=6000;

// ── CINEMA ────────────────────────────────────────────────────
function buildCinema(works){
  const v=document.getElementById('cinematicViewer');
  v.querySelectorAll('.cinema-slide').forEach(s=>s.remove());
  works.forEach((a,i)=>{
    const s=document.createElement('div');
    s.className='cinema-slide'+(i===0?' active':'');
    s.innerHTML=`<div class="cinema-bg" style="background:${getBg(a)};"></div>
    <div class="cinema-overlay"></div>
    <div class="cinema-content">
      <div class="cinema-category">${getCategoryName(a.category)}</div>
      <div class="cinema-title">${a.title}</div>
      <div class="cinema-meta">${[a.medium,a.size,a.year].filter(Boolean).join(' · ')}</div>
      <div class="cinema-desc">${a.desc}</div>
      <div class="cinema-actions">${a.sold
        ?`<span class="sold-badge">● Sold</span><button class="btn-ghost" onclick="openInquiry('${a.id}','similar')">Inquire About Similar →</button>`
        :`<button class="btn-gold" onclick="openInquiry('${a.id}','purchase')">Inquire to Purchase →</button><button class="btn-ghost" onclick="openInquiry(null,'commission')">Commission Similar →</button>`
      }</div>
    </div>`;
    v.insertBefore(s, v.querySelector('.cinema-nav'));
  });
  document.getElementById('counterTotal').textContent=`of ${String(works.length).padStart(2,'0')}`;
  updateCounter(0);
}

function buildThumbs(works){
  const strip=document.getElementById('thumbStrip');
  strip.innerHTML='';
  works.forEach((a,i)=>{
    const t=document.createElement('div');
    t.className='thumb'+(i===0?' active':'');
    t.onclick=()=>goToSlide(i);
    t.innerHTML=`<div class="thumb-bg" style="background:${getThumb(a)};"></div>${a.sold?'<div class="thumb-sold">Sold</div>':''}`;
    strip.appendChild(t);
  });
}

function buildGrid(works){
  const g=document.getElementById('portfolioGrid');
  g.innerHTML='';
  works.forEach(a=>{
    const el=document.createElement('div');
    el.className='grid-item';
    el.onclick=()=>openLightbox(a.id,works);
    el.innerHTML=`<div class="grid-item-bg" style="background:${getBg(a)};"></div>
    <div class="grid-cat">${getCategoryShort(a.category)}</div>
    ${a.sold?'<div class="grid-sold">Sold</div>':''}
    <div class="grid-zoom"><div class="grid-zoom-icon">⤢</div></div>
    <div class="grid-item-info"><h3>${a.title}</h3><p>${[a.medium,a.year].filter(Boolean).join(' · ')}</p></div>`;
    g.appendChild(el);
  });
}

function buildChaseGrid(){
  const featured=artworks.filter(a=>a.featured);
  const g=document.getElementById('chaseGrid');
  if(!g)return;
  g.innerHTML='';
  featured.slice(0,6).forEach(a=>{
    const el=document.createElement('div');
    el.className='series-item';
    el.onclick=()=>openLightbox(a.id,featured);
    el.innerHTML=`<div class="series-item-bg" style="background:${getBg(a)};"></div>
    <div class="series-item-overlay"><div><h3>${a.title}</h3><p>${[a.medium,a.year].filter(Boolean).join(' · ')}${a.sold?' · SOLD':''}</p></div></div>`;
    g.appendChild(el);
  });
  const first=featured[0];
  if(first) document.getElementById('seriesHeroImg').style.background=getBg(first);
}

function buildTicker(){
  const wrap=document.getElementById('tickerWrap');
  wrap.innerHTML='';
  const rowSize=Math.ceil(customerPhotos.length/3);
  const rows=[customerPhotos.slice(0,rowSize),customerPhotos.slice(rowSize,rowSize*2),customerPhotos.slice(rowSize*2)];
  rows.forEach((photos,ri)=>{
    const row=document.createElement('div');
    row.className='ticker-row';
    const track=document.createElement('div');
    track.className='ticker-track'+(ri%2===1?' reverse':'');
    [...photos,...photos].forEach(url=>{
      const cell=document.createElement('div');
      cell.className='ticker-cell';
      cell.innerHTML=`<img src="${url}" loading="lazy" alt="Happy collector"/><div class="ticker-overlay"></div><div class="ticker-gold"></div>`;
      track.appendChild(cell);
    });
    row.appendChild(track);
    wrap.appendChild(row);
  });
}

function buildProjects(){
  const g=document.getElementById('projectsGrid');
  g.innerHTML='';
  projects.forEach(p=>{
    const card=document.createElement('div');
    card.className=`project-card ${p.layout}`;
    card.innerHTML=`<div class="project-bg" style="background:url('${p.url}') center/cover no-repeat;"></div>
    <div class="project-info"><div class="project-type">${p.type}</div><h3>${p.title}</h3><p>${p.desc}</p></div>`;
    g.appendChild(card);
  });
}

// ── SLIDE CONTROLS ────────────────────────────────────────────
function goToSlide(n){
  const slides=document.querySelectorAll('.cinema-slide');
  const thumbs=document.querySelectorAll('.thumb');
  slides[currentSlide]?.classList.remove('active');
  thumbs[currentSlide]?.classList.remove('active');
  currentSlide=(n+slides.length)%slides.length;
  slides[currentSlide]?.classList.add('active');
  thumbs[currentSlide]?.classList.add('active');
  thumbs[currentSlide]?.scrollIntoView({behavior:'smooth',block:'nearest',inline:'center'});
  updateCounter(currentSlide);
  resetProgress();
}
function changeSlide(dir){goToSlide(currentSlide+dir);}
function updateCounter(n){document.getElementById('counterCurrent').textContent=String(n+1).padStart(2,'0');}
function resetProgress(){
  clearInterval(progressTimer); progressWidth=0;
  document.getElementById('progressFill').style.width='0%';
  progressTimer=setInterval(()=>{progressWidth+=100/(SLIDE_DURATION/100);document.getElementById('progressFill').style.width=Math.min(progressWidth,100)+'%';},100);
}
function startAutoPlay(){clearInterval(autoPlay);autoPlay=setInterval(()=>changeSlide(1),SLIDE_DURATION);resetProgress();}

function filterGallery(cat,btn){
  document.querySelectorAll('.filter-btn').forEach(b=>b.classList.remove('active'));
  btn.classList.add('active');
  filteredArtworks=cat==='all'?[...artworks]:artworks.filter(a=>a.category===cat);
  currentSlide=0; buildCinema(filteredArtworks); buildThumbs(filteredArtworks); buildGrid(filteredArtworks); startAutoPlay();
}

// ── LIGHTBOX ──────────────────────────────────────────────────
function openLightbox(id,works){
  lbWorks=works||filteredArtworks; currentLbIndex=lbWorks.findIndex(a=>a.id===id);
  renderLightbox(currentLbIndex); document.getElementById('lightbox').classList.add('open'); document.body.style.overflow='hidden';
}
function openLightboxFromSlide(){openLightbox(filteredArtworks[currentSlide].id,filteredArtworks);}
function closeLightbox(e){
  if(e&&e.target!==document.getElementById('lightbox'))return;
  document.getElementById('lightbox').classList.remove('open'); document.body.style.overflow='';
}
function lbNav(dir){currentLbIndex=(currentLbIndex+dir+lbWorks.length)%lbWorks.length;renderLightbox(currentLbIndex);}
function renderLightbox(i){
  const a=lbWorks[i];
  document.getElementById('lbArt').style.background=getBg(a);
  document.getElementById('lbCat').textContent=getCategoryName(a.category);
  document.getElementById('lbTitle').textContent=a.title;
  document.getElementById('lbMeta').textContent=[a.medium,a.size,a.year].filter(Boolean).join(' · ');
  document.getElementById('lbDesc').textContent=a.desc;
  document.getElementById('lbPrice').textContent=a.price;
  document.getElementById('lbPriceNote').textContent=a.sold?'':'Shipping included';
  document.getElementById('lbArt').querySelector('.lb-art-sold')?.remove();
  if(a.sold){const b=document.createElement('div');b.className='lb-art-sold';b.textContent='Sold';document.getElementById('lbArt').appendChild(b);}
  document.getElementById('lbActions').innerHTML=a.sold
    ?`<button class="btn-ghost" onclick="openInquiry('${a.id}','similar')">Inquire About Similar →</button>`
    :`<button class="btn-gold" onclick="openInquiry('${a.id}','purchase')">Inquire to Purchase →</button>
      <button class="btn-ghost" onclick="openInquiry(null,'commission')">Commission Similar →</button>`;
}

// ── INQUIRY ───────────────────────────────────────────────────
function openInquiry(id,type){
  document.getElementById('inqForm').style.display='';
  document.getElementById('inqSuccess').style.display='none';
  document.getElementById('inqType').value=(type==='purchase'||type==='similar')?'purchase':type==='commission'?'commission':type==='mural'?'mural':'general';
  const f=document.getElementById('inqFor');
  if(id){const a=artworks.find(x=>x.id===id);if(a){f.style.display='';f.innerHTML=`Regarding: <strong>${a.title}</strong> · ${a.medium}${a.size?' · '+a.size:''}${a.sold?' <span style="color:#e55;">· Sold</span>':''}`;}else f.style.display='none';}
  else f.style.display='none';
  document.getElementById('inquiryModal').classList.add('open'); document.body.style.overflow='hidden';
  document.getElementById('lightbox').classList.remove('open');
}
function closeInquiry(e){
  if(e&&e.target!==document.getElementById('inquiryModal'))return;
  document.getElementById('inquiryModal').classList.remove('open'); document.body.style.overflow='';
}

// ── EVENT LISTENERS ───────────────────────────────────────────
document.getElementById('prevBtn').onclick=()=>changeSlide(-1);
document.getElementById('nextBtn').onclick=()=>changeSlide(1);
document.getElementById('fullViewBtn').onclick=openLightboxFromSlide;
document.getElementById('lbCloseBtn').onclick=()=>closeLightbox({target:document.getElementById('lightbox')});
document.getElementById('lbPrevBtn').onclick=()=>lbNav(-1);
document.getElementById('lbNextBtn').onclick=()=>lbNav(1);
document.getElementById('lightbox').onclick=closeLightbox;
document.getElementById('lbInner').onclick=e=>e.stopPropagation();
document.getElementById('inqCloseBtn').onclick=()=>closeInquiry({target:document.getElementById('inquiryModal')});
document.getElementById('inquiryModal').onclick=closeInquiry;
document.getElementById('inqBox').onclick=e=>e.stopPropagation();
document.getElementById('inqSubmitBtn').onclick=()=>{
  if(!document.getElementById('inqFirst').value.trim()||!document.getElementById('inqEmail').value.trim()){alert('Please fill in your name and email.');return;}
  document.getElementById('inqForm').style.display='none'; document.getElementById('inqSuccess').style.display='block';
  setTimeout(()=>{document.getElementById('inquiryModal').classList.remove('open');document.body.style.overflow='';},3500);
};
document.getElementById('submitPhotoBtn').onclick=()=>openInquiry(null,'general');
document.getElementById('muralInquiryBtn').onclick=()=>openInquiry(null,'mural');
document.querySelectorAll('.filter-btn').forEach(btn=>{
  btn.onclick=()=>filterGallery(btn.dataset.cat,btn);
});

document.addEventListener('keydown',e=>{
  if(document.getElementById('lightbox').classList.contains('open')){
    if(e.key==='ArrowLeft')lbNav(-1); if(e.key==='ArrowRight')lbNav(1);
    if(e.key==='Escape')closeLightbox({target:document.getElementById('lightbox')}); return;
  }
  if(e.key==='ArrowUp'||e.key==='ArrowLeft')changeSlide(-1);
  if(e.key==='ArrowDown'||e.key==='ArrowRight')changeSlide(1);
  if(e.key==='Escape'){document.getElementById('inquiryModal').classList.remove('open');document.body.style.overflow='';}
});

const ro=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible');}),{threshold:0.15});
document.querySelectorAll('.reveal').forEach(el=>ro.observe(el));

let tx=0,ty=0;
document.getElementById('cinematicViewer').addEventListener('touchstart',e=>{tx=e.touches[0].clientX;ty=e.touches[0].clientY;},{passive:true});
document.getElementById('cinematicViewer').addEventListener('touchend',e=>{
  const dx=e.changedTouches[0].clientX-tx,dy=e.changedTouches[0].clientY-ty;
  if(Math.abs(dx)>Math.abs(dy)&&Math.abs(dx)>40)changeSlide(dx<0?1:-1);
},{passive:true});
document.getElementById('cinematicViewer').addEventListener('click',()=>{clearInterval(autoPlay);clearInterval(progressTimer);});

// ── INIT ──────────────────────────────────────────────────────
buildCinema(artworks); buildThumbs(artworks); buildGrid(artworks);
buildChaseGrid(); buildTicker(); buildProjects(); startAutoPlay();
</script>

<style is:global>
  /* HERO */
  .hero{padding:9rem 3rem 5rem;text-align:center;background:linear-gradient(135deg,#080808,#0f0800,#080808);position:relative;overflow:hidden;}
  .hero::before{content:'';position:absolute;inset:0;background:radial-gradient(ellipse at 50% 0%,rgba(201,168,76,0.07),transparent 65%);}
  .hero-tag{display:inline-flex;align-items:center;gap:0.8rem;margin-bottom:1.5rem;animation:fadeUp 0.8s 0.2s both;}
  .hero-line{width:30px;height:1px;background:var(--gold);}
  .hero-tag span{font-size:0.68rem;letter-spacing:4px;text-transform:uppercase;color:var(--gold);}
  .hero h1{font-family:'Playfair Display',serif;font-size:clamp(2.8rem,5vw,4.5rem);line-height:1.1;margin-bottom:1rem;animation:fadeUp 0.8s 0.4s both;}
  .hero h1 em{color:var(--gold);font-style:italic;}
  .hero p{color:var(--gray2);max-width:560px;margin:0 auto;font-size:0.95rem;line-height:1.9;font-weight:300;animation:fadeUp 0.8s 0.6s both;}
  @keyframes fadeUp{from{opacity:0;transform:translateY(20px);}to{opacity:1;transform:translateY(0);}}

  /* FILTER */
  .filter-bar{display:flex;justify-content:center;gap:0.5rem;flex-wrap:wrap;padding:0 3rem 3rem;background:linear-gradient(135deg,#080808,#0f0800,#080808);}
  .filter-btn{background:transparent;color:var(--gray2);border:1px solid rgba(255,255,255,0.08);padding:0.6rem 1.4rem;font-size:0.72rem;letter-spacing:2px;text-transform:uppercase;cursor:pointer;transition:all 0.3s;font-family:'Inter',sans-serif;}
  .filter-btn:hover,.filter-btn.active{background:var(--gold);color:var(--black);border-color:var(--gold);font-weight:600;}

  /* CINEMATIC */
  .cinematic{position:relative;height:90vh;overflow:hidden;background:var(--black);}
  .cinema-slide{position:absolute;inset:0;opacity:0;transition:opacity 0.9s ease;pointer-events:none;}
  .cinema-slide.active{opacity:1;pointer-events:all;}
  .cinema-bg{position:absolute;inset:0;background-size:cover;background-position:center top;filter:brightness(0.55);transform:scale(1.05);transition:transform 8s ease;}
  .cinema-slide.active .cinema-bg{transform:scale(1);}
  .cinema-overlay{position:absolute;inset:0;background:linear-gradient(to right,rgba(8,8,8,0.95) 0%,rgba(8,8,8,0.4) 50%,rgba(8,8,8,0.05) 100%);}
  .cinema-content{position:absolute;bottom:0;left:0;top:0;display:flex;flex-direction:column;justify-content:flex-end;padding:4rem 5rem;max-width:580px;z-index:2;}
  .cinema-category{font-size:0.65rem;letter-spacing:4px;text-transform:uppercase;color:var(--gold);margin-bottom:0.8rem;display:flex;align-items:center;gap:0.8rem;}
  .cinema-category::before{content:'';width:20px;height:1px;background:var(--gold);}
  .cinema-title{font-family:'Playfair Display',serif;font-size:clamp(2rem,4vw,3.5rem);line-height:1.1;margin-bottom:0.8rem;}
  .cinema-meta{color:var(--gray2);font-size:0.82rem;letter-spacing:1px;margin-bottom:1rem;}
  .cinema-desc{color:var(--gray2);font-size:0.88rem;line-height:1.8;margin-bottom:2rem;font-weight:300;max-width:420px;}
  .cinema-actions{display:flex;gap:1rem;align-items:center;flex-wrap:wrap;}
  .btn-gold{background:var(--gold);color:var(--black);padding:0.75rem 1.8rem;font-size:0.75rem;letter-spacing:2px;text-transform:uppercase;font-weight:700;border:none;cursor:pointer;transition:all 0.3s;}
  .btn-gold:hover{background:var(--gold-light);}
  .btn-ghost{background:transparent;color:var(--white);padding:0.75rem 1.8rem;font-size:0.75rem;letter-spacing:2px;text-transform:uppercase;border:1px solid rgba(255,255,255,0.2);cursor:pointer;transition:all 0.3s;}
  .btn-ghost:hover{border-color:var(--gold);color:var(--gold);}
  .sold-badge{background:rgba(180,30,30,0.9);color:#fff;font-size:0.65rem;letter-spacing:2px;text-transform:uppercase;padding:0.35rem 0.8rem;font-weight:600;}
  .cinema-fullview{position:absolute;top:1.5rem;right:7rem;z-index:10;background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.1);color:var(--white);padding:0.5rem 1rem;font-size:0.68rem;letter-spacing:2px;text-transform:uppercase;cursor:pointer;backdrop-filter:blur(10px);transition:all 0.3s;}
  .cinema-fullview:hover{background:var(--gold);border-color:var(--gold);color:var(--black);}
  .cinema-nav{position:absolute;right:3rem;top:50%;transform:translateY(-50%);display:flex;flex-direction:column;gap:1rem;z-index:10;}
  .cinema-arrow{width:48px;height:48px;background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.1);color:var(--white);display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all 0.3s;font-size:1rem;backdrop-filter:blur(10px);}
  .cinema-arrow:hover{background:var(--gold);border-color:var(--gold);color:var(--black);}
  .cinema-progress{position:absolute;bottom:0;left:0;right:0;height:2px;background:rgba(255,255,255,0.06);z-index:10;}
  .cinema-progress-fill{height:100%;background:var(--gold);width:0;transition:width 0.1s linear;}
  .cinema-counter{position:absolute;right:3rem;bottom:3rem;z-index:10;text-align:right;}
  .counter-current{font-family:'Playfair Display',serif;font-size:3rem;color:var(--gold);line-height:1;}
  .counter-total{font-size:0.7rem;letter-spacing:2px;color:var(--gray);text-transform:uppercase;}

  /* THUMBS */
  .thumb-strip{background:#0a0a0a;padding:1.5rem 3rem;display:flex;gap:3px;overflow-x:auto;scrollbar-width:none;}
  .thumb-strip::-webkit-scrollbar{display:none;}
  .thumb{width:80px;height:60px;flex-shrink:0;cursor:pointer;position:relative;overflow:hidden;opacity:0.4;transition:opacity 0.3s,transform 0.3s;}
  .thumb:hover,.thumb.active{opacity:1;transform:scaleY(1.05);}
  .thumb-bg{width:100%;height:100%;background-size:cover;background-position:center top;}
  .thumb-sold{position:absolute;inset:0;background:rgba(180,30,30,0.6);display:flex;align-items:center;justify-content:center;font-size:0.5rem;letter-spacing:1px;text-transform:uppercase;color:#fff;}

  /* STATS */
  .stats-bar{background:#0c0900;border-top:1px solid rgba(201,168,76,0.1);border-bottom:1px solid rgba(201,168,76,0.1);padding:3rem;display:grid;grid-template-columns:repeat(4,1fr);text-align:center;gap:2rem;}
  .stat-num{font-family:'Playfair Display',serif;font-size:2.5rem;color:var(--gold);line-height:1;margin-bottom:0.3rem;}
  .stat-label{font-size:0.68rem;letter-spacing:3px;text-transform:uppercase;color:var(--gray2);}

  /* GRID */
  .grid-section{padding:6rem 3rem;background:var(--dark);}
  .grid-header{display:flex;justify-content:space-between;align-items:flex-end;margin-bottom:3rem;flex-wrap:wrap;gap:1rem;}
  .grid-header h2{font-family:'Playfair Display',serif;font-size:2rem;}
  .portfolio-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:3px;}
  .grid-item{position:relative;aspect-ratio:3/4;overflow:hidden;cursor:pointer;}
  .grid-item-bg{width:100%;height:100%;background-size:cover;background-position:center top;transition:transform 0.6s,filter 0.4s;filter:brightness(0.8);}
  .grid-item:hover .grid-item-bg{transform:scale(1.06);filter:brightness(1);}
  .grid-item-info{position:absolute;bottom:0;left:0;right:0;padding:1.2rem;background:linear-gradient(to top,rgba(0,0,0,0.92),transparent);transform:translateY(100%);transition:transform 0.35s;}
  .grid-item:hover .grid-item-info{transform:translateY(0);}
  .grid-item-info h3{font-family:'Playfair Display',serif;font-size:0.9rem;margin-bottom:0.2rem;}
  .grid-item-info p{font-size:0.65rem;letter-spacing:1px;text-transform:uppercase;color:var(--gray2);}
  .grid-sold{position:absolute;top:0.8rem;right:0.8rem;background:rgba(180,30,30,0.9);color:#fff;font-size:0.55rem;letter-spacing:1.5px;text-transform:uppercase;padding:0.25rem 0.6rem;}
  .grid-cat{position:absolute;top:0.8rem;left:0.8rem;background:rgba(8,8,8,0.7);color:var(--gold);font-size:0.55rem;letter-spacing:1.5px;text-transform:uppercase;padding:0.25rem 0.6rem;border:1px solid rgba(201,168,76,0.3);}
  .grid-zoom{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;opacity:0;transition:opacity 0.3s;}
  .grid-item:hover .grid-zoom{opacity:1;}
  .grid-zoom-icon{width:44px;height:44px;border:1px solid rgba(255,255,255,0.5);display:flex;align-items:center;justify-content:center;font-size:1.2rem;backdrop-filter:blur(4px);background:rgba(0,0,0,0.3);}

  /* SERIES */
  .series{padding:6rem 3rem;background:var(--black);}
  .series-intro{display:grid;grid-template-columns:1fr 1fr;gap:5rem;align-items:center;margin-bottom:4rem;}
  .series-intro h2{font-family:'Playfair Display',serif;font-size:2.2rem;margin-bottom:1rem;}
  .series-intro p{color:var(--gray2);font-size:0.93rem;line-height:1.9;font-weight:300;}
  .series-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:3px;}
  .series-item{position:relative;aspect-ratio:1;overflow:hidden;cursor:pointer;}
  .series-item-bg{width:100%;height:100%;background-size:cover;background-position:center top;transition:transform 0.6s,filter 0.4s;filter:brightness(0.75);}
  .series-item:hover .series-item-bg{transform:scale(1.06);filter:brightness(1);}
  .series-item-overlay{position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.85),transparent 50%);display:flex;align-items:flex-end;padding:1.2rem;}
  .series-item-overlay h3{font-family:'Playfair Display',serif;font-size:0.95rem;}
  .series-item-overlay p{font-size:0.65rem;letter-spacing:1px;text-transform:uppercase;color:var(--gray2);margin-top:0.2rem;}

  /* COLLECTORS */
  .collectors{padding:6rem 3rem;background:var(--black);overflow:hidden;}
  .collectors-header{display:grid;grid-template-columns:1fr 1fr;gap:4rem;align-items:end;margin-bottom:3.5rem;}
  .collectors-header h2{font-family:'Playfair Display',serif;font-size:2.2rem;line-height:1.2;}
  .collectors-header h2 em{color:var(--gold);font-style:italic;}
  .collectors-sub{color:var(--gray2);font-size:0.93rem;line-height:1.9;font-weight:300;align-self:end;}
  .ticker-wrap{display:flex;flex-direction:column;gap:3px;margin-bottom:3rem;}
  .ticker-row{display:flex;gap:3px;overflow:hidden;}
  .ticker-track{display:flex;gap:3px;animation:ticker 40s linear infinite;}
  .ticker-track.reverse{animation-direction:reverse;animation-duration:50s;}
  .ticker-cell{width:160px;height:160px;flex-shrink:0;position:relative;overflow:hidden;cursor:pointer;}
  .ticker-cell img{width:100%;height:100%;object-fit:cover;filter:brightness(0.85);transition:filter 0.3s,transform 0.4s;}
  .ticker-cell:hover img{filter:brightness(1);transform:scale(1.08);}
  .ticker-overlay{position:absolute;inset:0;background:rgba(0,0,0,0);transition:background 0.3s;display:flex;align-items:center;justify-content:center;}
  .ticker-cell:hover .ticker-overlay{background:rgba(201,168,76,0.15);}
  .ticker-gold{position:absolute;bottom:0;left:0;right:0;height:2px;background:var(--gold);transform:scaleX(0);transition:transform 0.3s;}
  .ticker-cell:hover .ticker-gold{transform:scaleX(1);}
  @keyframes ticker{from{transform:translateX(0);}to{transform:translateX(-50%)}}
  .ticker-wrap:hover .ticker-track{animation-play-state:paused;}

  /* BEYOND */
  .beyond{padding:6rem 3rem;background:var(--dark);}
  .beyond-header{margin-bottom:3.5rem;}
  .beyond-header h2{font-family:'Playfair Display',serif;font-size:2.2rem;margin-bottom:0.8rem;}
  .beyond-header h2 em{color:var(--gold);font-style:italic;}
  .beyond-header p{color:var(--gray2);font-size:0.93rem;line-height:1.9;font-weight:300;max-width:600px;}
  .projects-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:3px;}
  .project-card{position:relative;overflow:hidden;cursor:pointer;}
  .project-card.wide{grid-column:span 2;aspect-ratio:16/9;}
  .project-card.sq{aspect-ratio:1;}
  .project-bg{width:100%;height:100%;min-height:260px;background-size:cover;background-position:center;filter:brightness(0.7);transition:filter 0.4s,transform 0.6s;}
  .project-card:hover .project-bg{filter:brightness(1);transform:scale(1.04);}
  .project-info{position:absolute;bottom:0;left:0;right:0;padding:1.5rem;background:linear-gradient(to top,rgba(0,0,0,0.92),transparent);}
  .project-type{font-size:0.6rem;letter-spacing:3px;text-transform:uppercase;color:var(--gold);margin-bottom:0.4rem;}
  .project-info h3{font-family:'Playfair Display',serif;font-size:1.1rem;margin-bottom:0.2rem;}
  .project-info p{font-size:0.72rem;color:var(--gray2);}
  .beyond-cta{text-align:center;padding:3rem;background:rgba(201,168,76,0.04);border:1px solid rgba(201,168,76,0.1);margin-top:3px;}
  .beyond-cta h3{font-family:'Playfair Display',serif;font-size:1.5rem;margin-bottom:0.5rem;}
  .beyond-cta p{color:var(--gray2);font-size:0.88rem;margin-bottom:1.5rem;}

  /* COMMISSION BAND */
  .comm-band{padding:6rem 3rem;background:linear-gradient(135deg,#0d0800,#150900);display:grid;grid-template-columns:1fr 1fr;gap:5rem;align-items:center;}
  .comm-band h2{font-family:'Playfair Display',serif;font-size:2.2rem;margin-bottom:1rem;line-height:1.2;}
  .comm-band h2 em{color:var(--gold);font-style:italic;}
  .comm-band p{color:var(--gray2);font-size:0.93rem;line-height:1.9;font-weight:300;margin-bottom:2rem;}
  .comm-steps-mini{display:flex;flex-direction:column;gap:1rem;}
  .comm-step-mini{display:flex;align-items:center;gap:1rem;}
  .step-dot{width:32px;height:32px;border:1px solid var(--gold);display:flex;align-items:center;justify-content:center;font-size:0.65rem;color:var(--gold);flex-shrink:0;}
  .comm-step-mini p{color:var(--gray2);font-size:0.85rem;}

  /* LIGHTBOX */
  .lightbox{position:fixed;inset:0;z-index:1000;background:rgba(0,0,0,0);pointer-events:none;transition:background 0.4s;display:flex;align-items:center;justify-content:center;}
  .lightbox.open{background:rgba(0,0,0,0.94);pointer-events:all;}
  .lb-inner{display:flex;max-width:1100px;width:95%;max-height:90vh;opacity:0;transform:scale(0.95);transition:all 0.4s;background:var(--card);}
  .lightbox.open .lb-inner{opacity:1;transform:scale(1);}
  .lb-art{flex:1;min-height:500px;background-size:cover;background-position:center top;position:relative;}
  .lb-art-sold{position:absolute;top:1rem;left:1rem;background:rgba(180,30,30,0.9);color:#fff;font-size:0.62rem;letter-spacing:2px;text-transform:uppercase;padding:0.3rem 0.7rem;}
  .lb-info{width:320px;flex-shrink:0;padding:2.5rem;display:flex;flex-direction:column;justify-content:space-between;overflow-y:auto;}
  .lb-cat{font-size:0.62rem;letter-spacing:4px;text-transform:uppercase;color:var(--gold);margin-bottom:1rem;display:flex;align-items:center;gap:0.6rem;}
  .lb-cat::before{content:'';width:16px;height:1px;background:var(--gold);}
  .lb-title{font-family:'Playfair Display',serif;font-size:1.8rem;line-height:1.15;margin-bottom:0.5rem;}
  .lb-meta{font-size:0.78rem;color:var(--gray2);margin-bottom:1.2rem;}
  .lb-divider{height:1px;background:rgba(255,255,255,0.07);margin:1.2rem 0;}
  .lb-desc{font-size:0.88rem;line-height:1.85;color:var(--gray2);font-weight:300;margin-bottom:1.5rem;}
  .lb-price-row{display:flex;align-items:center;gap:1rem;margin-bottom:1.5rem;}
  .lb-price{font-family:'Playfair Display',serif;font-size:1.5rem;color:var(--gold);}
  .lb-price-note{font-size:0.7rem;color:var(--gray);letter-spacing:1px;text-transform:uppercase;}
  .lb-actions{display:flex;flex-direction:column;gap:0.7rem;}
  .lb-close{position:absolute;top:1rem;right:1rem;width:40px;height:40px;background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.1);color:var(--white);font-size:1.1rem;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all 0.3s;z-index:10;}
  .lb-close:hover{background:var(--gold);color:var(--black);}
  .lb-nav{display:flex;gap:0.5rem;margin-bottom:1rem;}
  .lb-nav-btn{flex:1;padding:0.5rem;background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);color:var(--gray2);font-size:0.7rem;letter-spacing:1px;cursor:pointer;text-transform:uppercase;transition:all 0.3s;}
  .lb-nav-btn:hover{border-color:var(--gold);color:var(--gold);}

  /* INQUIRY */
  .inquiry-modal{position:fixed;inset:0;z-index:2000;background:rgba(0,0,0,0);pointer-events:none;transition:background 0.3s;display:flex;align-items:center;justify-content:center;}
  .inquiry-modal.open{background:rgba(0,0,0,0.9);pointer-events:all;}
  .inq-box{background:#111;border:1px solid rgba(201,168,76,0.2);max-width:520px;width:92%;padding:3rem;opacity:0;transform:translateY(20px);transition:all 0.4s;position:relative;max-height:90vh;overflow-y:auto;}
  .inquiry-modal.open .inq-box{opacity:1;transform:translateY(0);}
  .inq-title{font-family:'Playfair Display',serif;font-size:1.6rem;margin-bottom:0.4rem;}
  .inq-sub{font-size:0.8rem;color:var(--gray2);letter-spacing:1px;margin-bottom:2rem;}
  .inq-for{background:rgba(201,168,76,0.06);border-left:2px solid var(--gold);padding:0.8rem 1rem;margin-bottom:2rem;font-size:0.85rem;color:var(--gray2);}
  .form-row{margin-bottom:1.2rem;}
  .form-row label{display:block;font-size:0.68rem;letter-spacing:2px;text-transform:uppercase;color:var(--gray2);margin-bottom:0.5rem;}
  .form-row input,.form-row textarea,.form-row select{width:100%;background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.1);color:var(--white);padding:0.75rem 1rem;font-family:'Inter',sans-serif;font-size:0.88rem;outline:none;transition:border-color 0.3s;}
  .form-row input:focus,.form-row textarea:focus,.form-row select:focus{border-color:var(--gold);}
  .form-row textarea{min-height:100px;resize:vertical;}
  .form-row select option{background:#111;}
  .form-grid{display:grid;grid-template-columns:1fr 1fr;gap:1rem;}
  .inq-close{position:absolute;top:1rem;right:1rem;width:36px;height:36px;background:transparent;border:1px solid rgba(255,255,255,0.1);color:var(--gray2);cursor:pointer;font-size:1rem;display:flex;align-items:center;justify-content:center;transition:all 0.3s;}
  .inq-close:hover{border-color:var(--gold);color:var(--gold);}
  .form-success{text-align:center;padding:2rem 0;}
  .form-success .check{font-size:3rem;margin-bottom:1rem;color:var(--gold);}
  .form-success h3{font-family:'Playfair Display',serif;font-size:1.5rem;margin-bottom:0.5rem;}
  .form-success p{color:var(--gray2);font-size:0.88rem;line-height:1.7;}

  /* SHARED */
  .label{font-size:0.68rem;letter-spacing:4px;text-transform:uppercase;color:var(--gold);margin-bottom:0.5rem;}
  .bar{width:50px;height:2px;background:var(--gold);margin-bottom:1.5rem;}
  .reveal{opacity:0;transform:translateY(30px);transition:opacity 0.7s,transform 0.7s;}
  .reveal.visible{opacity:1;transform:translateY(0);}

  /* RESPONSIVE */
  @media(max-width:900px){
    .hero,.filter-bar{padding-inline:1.5rem;}
    .cinematic{height:70vh;}
    .cinema-content{padding:2rem 1.5rem;max-width:100%;}
    .cinema-title{font-size:1.6rem;}
    .cinema-desc{display:none;}
    .cinema-nav{right:1rem;}
    .cinema-counter{right:1rem;bottom:1.5rem;}
    .counter-current{font-size:2rem;}
    .cinema-fullview{display:none;}
    .thumb-strip{padding:1rem 1.5rem;}
    .stats-bar{grid-template-columns:repeat(2,1fr);padding:2rem 1.5rem;}
    .grid-section,.series,.collectors,.beyond,.comm-band{padding:4rem 1.5rem;}
    .portfolio-grid{grid-template-columns:repeat(2,1fr);}
    .series-intro{grid-template-columns:1fr;gap:2rem;}
    .series-grid{grid-template-columns:repeat(2,1fr);}
    .collectors-header{grid-template-columns:1fr;gap:1.5rem;}
    .ticker-cell{width:120px;height:120px;}
    .projects-grid{grid-template-columns:1fr 1fr;}
    .project-card.wide{grid-column:span 2;}
    .comm-band{grid-template-columns:1fr;gap:3rem;}
    .lb-inner{flex-direction:column;max-height:95vh;overflow-y:auto;}
    .lb-art{min-height:280px;flex:none;}
    .lb-info{width:100%;}
  }
  @media(max-width:480px){
    .portfolio-grid{grid-template-columns:repeat(2,1fr);}
    .ticker-cell{width:90px;height:90px;}
    .projects-grid{grid-template-columns:1fr;}
    .project-card.wide{grid-column:span 1;}
    .form-grid{grid-template-columns:1fr;}
  }
</style>
